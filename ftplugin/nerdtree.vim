
" buffer local highlighting not working, mode to theme
" hi CursorLine ctermfg=236 ctermbg=150 guifg=#303030 guibg=#afd787

" nnoremap <buffer> <cr> :call nerdtree#ui_glue#invokeKeyMap("t")<CR>
nnoremap <buffer> <2-LeftMouse> :call nerdtree#ui_glue#invokeKeyMap("t")<CR>

" TODO: only open ONE tab for each file
" if file already open, switch to that tab


